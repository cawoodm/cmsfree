{
  "name": "cmsfree",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "node scripts/serve-static.mjs example-site/publish 4173",
    "publish": "pwsh -NoProfile -File ./publish.ps1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "marked": "^15.0.12"
  }
}