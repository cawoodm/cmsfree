# TODO

Mark tasks done with ✅ or in-progress with 🕜

- ✅ Esc should close and discard editor changes
- ✅ Ctrl+Enter should OK editor changes
- ✅ Create a "Done" button in the header which exits edit mode
- ✅ Create a publish.ps1 script which copies the example site to ../cawoodm.github.io/cmsfree-example, commits and pushes that repo (add script to package.json)
- Create a favicon.ico
